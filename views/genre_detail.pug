extends layout

block content
  h1= title
  div
    dt= genre.name
    dd= genre.description
  ul 
    each serie in serie_list
      li
        a(href=serie.url)=serie.title 
  a(href="/genre/" + genre._id + "/delete") Delete Genre
  a(href="/genre/" + genre._id + "/update") Update Genre